To install itrace, do the following:
- unpack:
	tar xvzf itrace-0.5c.tar.gz
- apply the patch
	cd itrace-0.5c 
	patch -p1 < ../itrace-0.5c.patch
- configure and build
	./configure && make
- install it as root:
	make install


